// check array subscript acccess (location)
//the subscript must be int.
//return type is the type of the array.

class Moshe  
{

	int x;
	int myfunc (string hello) 
	{
		string [] arr = new string [5];
		
		//expected: fail. access to array of type string,
		//then try to use the string with incompatible type.
		int y = (1 + arr[1]) / 8;
	}
	
}